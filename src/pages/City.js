import React from 'react'
import Header from '../components/global/Header'

function City({ title }) {
    return (
        <div className="wrapper">
            <Header title={ title } />
        </div>
    )
}

export default City
